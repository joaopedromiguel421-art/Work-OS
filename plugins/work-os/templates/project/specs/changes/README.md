# Active change specs

Create one kebab-case folder per non-trivial change. Use the templates in `specs/templates/`. A change folder normally contains `spec.md`, `plan.md`, and `tasks.md`; add research or rollout evidence only when needed.
